all:
    hosts:
        target:
            # trim guards against accidental leading/trailing whitespace or
            # newlines copied into the SSH_HOST secret.
            ansible_host: "{{ lookup('env', 'SSH_HOST') | trim }}"
            ansible_user: "{{ lookup('env', 'SSH_USERNAME') | trim }}"
            ansible_ssh_private_key_file: deploy_key
