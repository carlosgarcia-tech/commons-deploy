---
# Retries absorb transient Docker Hub resets on home connections.
- name: Pull {{ mongo_image }} image
  community.docker.docker_image:
    name: "{{ mongo_image }}"
    source: pull
  register: mongo_pull
  retries: 5
  delay: 15
  until: mongo_pull is succeeded
